pipeline {

  agent any
   stages {
     stage ('build'){
     steps { 
      sh  'echo  BUILD'  
        }   
     }
    }  
   post {
   always {
   archiveArtifacts artifacts: 'file.html', fingerprint: true
     } 
   } 
  }
