pipeline {

  agent any
   stages {
     stage ('build'){
     steps { 
      sh  'echo  BUILD'  
        }   
     }
    }  
   post {
    failure {
   archiveArtifacts artifacts: 'test/', fingerprint: true
     } 
   } 
  }
